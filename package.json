{
  "name": "alfoz-erp",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "bash start.sh",
    "build": "apt-get update && DEBIAN_FRONTEND=noninteractive apt-get install -y -o Dpkg::Options::=\"--force-confdef\" -o Dpkg::Options::=\"--force-confold\" php-cli php-mysql mariadb-server",
    "start": "bash start.sh"
  }
}
